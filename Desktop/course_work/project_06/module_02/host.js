const GUEST_LIST = [
    { name: 'Leonard', count: 6 },
    { name: 'Sharon', count: 4 }
];

function buildGuestCard(guest) {
    // Transform the guest objects into a a card
    // Return function
    <div class="guest-card">
        <span>${guest.name}, party of ${guest.count}</span>
    </div>

    return
}

function renderGuestList() {

} 